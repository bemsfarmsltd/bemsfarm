import TeamOnboarding from '../settings/TeamOnboarding'

/**
 * AddStaff — Streamlined Onboarding
 * Replaces the legacy multi-field form with the simple email & role invitation system under Settings.
 */
export default function AddStaff() {
  return <TeamOnboarding />
}
